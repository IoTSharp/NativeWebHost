<Project>
  <PropertyGroup Condition="'$(NativeWebHostRequireAdministrator)' == 'true' and '$(ApplicationManifest)' == ''">
    <ApplicationManifest>$(MSBuildThisFileDirectory)NativeWebHost.Windows.RequireAdministrator.manifest</ApplicationManifest>
  </PropertyGroup>
</Project>
